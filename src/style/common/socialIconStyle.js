const styles = theme => ({
    root: {
        width: 'auto',
        height: '1em',
        maxHeight: '100%',
        maxWidth: '100%',
        display: 'inline-block',
        fill: 'currentColor',
        overflow: 'hidden'
    }
});

export default styles;